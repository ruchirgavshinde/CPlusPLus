#include <iostream>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
	   int a1,a2,a3,c1,c2,c3;
	   
	   cin>>a1>>a2>>a3>>c1>>c2>>c3;
	   
	   if(a1>a2 && a1>a3)
	   {
	   	if(c1>c2 && c1>c3)
	   	{
	   		if(a2>a3 && c3>c3)
	   		cout<<"FAIR"<<endl;
			
			else if(a2<a3 && c2<c3)
			cout<<"FAIR"<<endl;
			
			else if(a2==a3 && c2==c3)
			cout<<"FAIR"<<endl;
			   
			else
			cout<<"NOT FAIR"<<endl;
		   }
		   else
		   cout<<"NOT FAIR";
	   }
	   else if(a2>a1 && a2>a3)
	   {
	   	if(c2>c1 && c2>c3)
	   	{
	   		if(a1>a3 && c1>c3)
	   		cout<<"FAIR"<<endl;
			
			else if(a1<a3 && c1<c3)
			cout<<"FAIR"<<endl;
			
			else if(a1==a3 && c1==c3)
			cout<<"FAIR"<<endl;
			   
			else
			cout<<"NOT FAIR"<<endl;
		   }
		   else
		   cout<<"NOT FAIR";
	   }
	    else if(a3>a1 && a3>a2)
	   {
	   	if(c3>c1 && c3>c2)
	   	{
	   		if(a1>a2 && c1>c2)
	   		cout<<"FAIR"<<endl;
			
			else if(a1<a2 && c1<c2)
			cout<<"FAIR"<<endl;
			
			else if(a1==a2 && c1==c2)
			cout<<"FAIR"<<endl;
			   
			else
			cout<<"NOT FAIR"<<endl;
		   }
		   else
		   cout<<"NOT FAIR";
	   }
	   else if(a1==a2 && a2==a3 && c1==c2 && c2==c3)
	   cout<<"FAIR"<<endl;
	   else 
	   cout<<"NOT FAIR"<<endl;
	}
}
